# Postgres
POSTGRES_USER=unclaimed
POSTGRES_PASSWORD=changeme
POSTGRES_DB=unclaimed
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Full connection string used by SQLAlchemy
DATABASE_URL=postgresql://unclaimed:changeme@db:5432/unclaimed

# Mobility Database API (https://api.mobilitydatabase.org/v1)
# Fill in once we confirm whether auth is required in Step 2
MOBILITY_DATABASE_API_KEY=

# API
API_PORT=8000
